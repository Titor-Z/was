name: CI Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          cache: true

      - run: cargo build --release

      - uses: actions/upload-artifact@v4
        with:
          name: was-x64-windows.exe
          path: target/release/was.exe
          if-no-files-found: error

      - uses: actions/upload-artifact@v4
        with:
          name: unwas-x64-windows.exe
          path: target/release/unwas.exe
          if-no-files-found: error
